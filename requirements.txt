torch>=2.3.0
numpy
einops
soundfile
huggingface_hub
librosa
tqdm
pypickle
# CoDiCodec is the upstream encoder/decoder. The local checkout is in ./codicodec.
# Install it editable with: pip install -e ./codicodec
